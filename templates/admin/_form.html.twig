{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success mb-2" onclick="return alert('Ajout/edition approuvee')">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
