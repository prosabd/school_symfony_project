{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Immatriculation</th>
                <td>{{ voiture.immatriculation }}</td>
            </tr>
            <tr>
                <th>Nombreportes</th>
                <td>{{ voiture.nombreportes }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ voiture.annee }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_index') }}" class="btn btn-secondary mb-2">back to list</a>

    <a href="{{ path('app_admin_edit', {'id': voiture.id}) }}" class="btn btn-warning mb-2">edit</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
